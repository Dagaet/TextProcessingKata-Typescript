export function textProcessing() {
  const text = "Hello, this is an example for you to practice. You should grab this text and make it as your test case."
  return "The text has in total " + text.split(" ").length + " words"
}
